h1 =t('hello')
= render 'layouts/header'
.nav_bar
  c
    p = link_to t('my_blog'), controller: 'articles'
    p = link_to t('new_article'), new_article_path
    p = link_to t('login.sign_up'), new_user_registration_path
    p = link_to t('login.sign_in_facebook'), user_omniauth_authorize_path(:facebook)
    p = link_to t('login.sign_in'), new_user_session_path
